import { X } from "lucide-react";import type { PropsWithChildren,ReactNode } from "react";
export function Drawer({open,title,onClose,children,footer}:{open:boolean;title:string;onClose:()=>void;children:ReactNode;footer?:ReactNode}){if(!open)return null;return <div className="drawer-backdrop" role="presentation" onMouseDown={(e)=>{if(e.target===e.currentTarget)onClose();}}><section className="drawer" role="dialog" aria-modal="true" aria-label={title}><header className="drawer-header"><h2>{title}</h2><button className="icon-button" onClick={onClose} aria-label="닫기" title="닫기"><X size={19}/></button></header><div className="drawer-body">{children}</div>{footer}</section></div>;}
export function DrawerSection({children}:PropsWithChildren){return <section className="result-section">{children}</section>;}
