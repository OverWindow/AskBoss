import { afterAll,describe,expect,it,vi } from "vitest";
import { buildApp } from "../src/app";
import { store } from "../src/repositories";
const app=buildApp();
afterAll(()=>app.close());
describe("session API",()=>{it("creates a browser session and exposes only its bosses",async()=>{const created=await app.inject({method:"POST",url:"/api/session"});expect(created.statusCode).toBe(200);const cookie=created.headers["set-cookie"] as string;expect(cookie).toContain("HttpOnly");expect(cookie).toContain("SameSite=Lax");const list=await app.inject({method:"GET",url:"/api/bosses",headers:{cookie:cookie.split(";")[0]!}});expect(list.statusCode).toBe(200);expect(list.json().bosses[0].alias).toBe("모두의 상사");});it("rejects requests without a session",async()=>{const response=await app.inject({method:"GET",url:"/api/bosses"});expect(response.statusCode).toBe(401);});it("does not crash a request when a background last-seen update fails",async()=>{const created=await app.inject({method:"POST",url:"/api/session"});const cookie=String(created.headers["set-cookie"]).split(";")[0]!;const now=Date.now();const clock=vi.spyOn(Date,"now").mockReturnValue(now+2*60_000);const touch=vi.spyOn(store,"touchSession").mockRejectedValue(Object.assign(new Error("statement timeout"),{code:"57014"}));const response=await app.inject({method:"GET",url:"/api/bosses",headers:{cookie}});await new Promise((resolve)=>setTimeout(resolve,0));expect(response.statusCode).toBe(200);expect(touch).toHaveBeenCalledTimes(1);touch.mockRestore();clock.mockRestore();});});
